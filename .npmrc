# Hoist all packages to root node_modules so the webpack-bundled API can
# resolve externalized deps (e.g. @prisma/client) via Node.js require() at runtime.
# Without this, pnpm's strict layout puts transitive deps in package-local
# node_modules that aren't on the runtime resolution path.
shamefully-hoist=true
